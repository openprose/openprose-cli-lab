# Shape-only schema examples

These immutable examples exercise closed schema shapes. They are not adapter
admission records, release profiles, benchmark policies, compatibility claims,
or language-owned artifacts. In particular, placeholder all-zero/all-`a`
digests do not identify releasable content.
