{
  "schema": "openprose.runner-case/1",
  "id": "transport.fake-nonzero",
  "summary": "A nonzero harness exit without a terminal record is still classified as protocol truncation.",
  "phase": 2,
  "tags": ["fake-process", "process-exit", "failure"],
  "controls": {
    "fixedInvocationId": "fixture-invocation-0001",
    "fixedTimestamp": "2025-01-01T00:00:00Z",
    "fixedMonotonicMs": 0,
    "network": "denied",
    "fakeHarness": { "scenario": "nonzero" }
  },
  "invocation": {
    "argv": ["--harness", "mock", "--transport", "fake-process", "--output", "json", "run", "fixture.prose.md"],
    "cwd": "{{WORKSPACE}}",
    "environment": {}
  },
  "expected": {
    "exitCode": 22,
    "stdout": { "kind": "json", "schema": "openprose.runner-result/1" },
    "stderr": { "kind": "empty" },
    "startedHarness": true,
    "errorCode": "PROTOCOL_TRUNCATED",
    "errorAction": "Retry once; if the problem persists, invoke the `cli doctor` runner operation and inspect sanitized diagnostics.",
    "fakeObservation": { "exactImageBytes": true, "exactTaskBytes": true }
  }
}
