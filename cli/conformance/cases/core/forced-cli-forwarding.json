{
  "schema": "openprose.runner-case/1",
  "id": "core.forced-cli-forwarding",
  "summary": "The delimiter forces cli through the opaque language path.",
  "phase": 1,
  "tags": ["argv", "reservation", "escape-hatch"],
  "controls": {
    "fixedInvocationId": "fixture-invocation-0001",
    "fixedTimestamp": "2025-01-01T00:00:00Z",
    "fixedMonotonicMs": 0,
    "network": "denied"
  },
  "invocation": {
    "argv": ["--harness", "mock", "--output", "json", "--", "cli", "doctor", "--json"],
    "cwd": "{{WORKSPACE}}",
    "environment": {}
  },
  "expected": {
    "exitCode": 0,
    "stdout": { "kind": "json", "schema": "openprose.runner-result/1" },
    "stderr": { "kind": "empty" },
    "startedHarness": true,
    "forwardedTask": {
      "argv": ["prose", "cli", "doctor", "--json"],
      "interactionMode": "non-interactive"
    }
  }
}
