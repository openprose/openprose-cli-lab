{
  "schema": "openprose.runner-case/1",
  "id": "core.config-roots-absent",
  "summary": "Absent ambient user-configuration roots fail closed.",
  "phase": 1,
  "tags": ["config", "environment", "fail-closed"],
  "controls": {
    "fixedInvocationId": "fixture-invocation-0001",
    "fixedTimestamp": "2025-01-01T00:00:00Z",
    "fixedMonotonicMs": 0,
    "network": "denied",
    "omitEnvironment": ["HOME", "XDG_CONFIG_HOME", "APPDATA"]
  },
  "invocation": {
    "argv": ["--output", "json", "cli", "config", "explain"],
    "cwd": "{{WORKSPACE}}",
    "environment": {}
  },
  "expected": {
    "exitCode": 2,
    "stdout": { "kind": "json", "schema": "openprose.runner-error/1" },
    "stderr": { "kind": "empty" },
    "startedHarness": false,
    "resultMatches": { "code": "CONFIG_INVALID", "boundary": "configuration" }
  }
}
