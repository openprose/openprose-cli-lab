{
  "schema": "openprose.runner-case/1",
  "id": "operations.config-invalid-harness-environment",
  "summary": "Both products reject an unsupported environment-selected harness at the configuration boundary.",
  "phase": 7,
  "tags": ["cli", "configuration", "agent-dx", "differential", "fail-closed"],
  "controls": {
    "fixedInvocationId": "fixture-invocation-0001",
    "fixedTimestamp": "2025-01-01T00:00:00Z",
    "fixedMonotonicMs": 0,
    "network": "denied"
  },
  "invocation": {
    "argv": ["--output", "json", "cli", "config", "explain"],
    "cwd": "{{WORKSPACE}}",
    "environment": {
      "HOME": "{{WORKSPACE}}/home",
      "XDG_CONFIG_HOME": "{{WORKSPACE}}/config",
      "PROSE_HARNESS": "nope"
    }
  },
  "expected": {
    "exitCode": 2,
    "stdout": { "kind": "json", "schema": "openprose.runner-error/1" },
    "stderr": { "kind": "empty" },
    "startedHarness": false,
    "resultMatches": {
      "code": "CONFIG_INVALID",
      "boundary": "configuration",
      "details": {
        "reason": "Unsupported harness \"nope\"; expected openprose, prime, omp, codex, claude, or mock.",
        "source": "PROSE_HARNESS"
      }
    }
  }
}
