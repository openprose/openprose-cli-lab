{
  "schema": "openprose.runner-case/1",
  "id": "operations.config-explain-default",
  "summary": "Both products expose the same source-attributed non-TTY configuration.",
  "phase": 6,
  "tags": ["cli", "configuration", "agent-dx", "differential"],
  "controls": {
    "fixedInvocationId": "fixture-invocation-0001",
    "fixedTimestamp": "2025-01-01T00:00:00Z",
    "fixedMonotonicMs": 0,
    "network": "denied"
  },
  "invocation": {
    "argv": ["--output", "json", "cli", "config", "explain"],
    "cwd": "{{WORKSPACE}}",
    "environment": {
      "HOME": "{{WORKSPACE}}/home",
      "XDG_CONFIG_HOME": "{{WORKSPACE}}/config"
    }
  },
  "expected": {
    "exitCode": 0,
    "stdout": { "kind": "json", "schema": "openprose.configuration-explanation/1" },
    "stderr": { "kind": "empty" },
    "startedHarness": false,
    "resultMatches": {
      "schema": "openprose.configuration-explanation/1",
      "projectConfigPath": null,
      "values": {
        "harness": { "value": "openprose" },
        "transport": { "value": "auto" },
        "model": { "value": null },
        "timeout": { "value": "10m" },
        "output": { "value": "json" },
        "color": { "value": false },
        "verbose": { "value": false },
        "authProfile": { "value": null }
      }
    }
  }
}
