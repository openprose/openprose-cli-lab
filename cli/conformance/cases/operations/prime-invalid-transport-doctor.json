{
  "schema": "openprose.runner-case/1",
  "id": "operations.prime-invalid-transport-doctor",
  "summary": "Doctor rejects an explicit unsupported installed-harness transport before inventory, authentication, or version probes.",
  "phase": 7,
  "tags": ["cli", "installed-adapter", "agent-dx", "differential", "fail-closed", "machine-output", "transport"],
  "controls": {
    "fixedInvocationId": "fixture-invocation-0001",
    "fixedTimestamp": "2025-01-01T00:00:00Z",
    "fixedMonotonicMs": 0,
    "network": "denied"
  },
  "invocation": {
    "argv": ["--harness", "prime", "--transport", "exec-json", "--model", "fixture/model", "--auth-profile", "prime-harness-login", "--output", "json", "cli", "doctor"],
    "cwd": "{{WORKSPACE}}",
    "environment": {}
  },
  "expected": {
    "exitCode": 20,
    "stdout": { "kind": "json", "schema": "openprose.runner-error/1" },
    "stderr": { "kind": "empty" },
    "startedHarness": false,
    "resultMatches": {
      "code": "TRANSPORT_UNSUPPORTED",
      "boundary": "adapter",
      "message": "The selected harness does not support the requested transport.",
      "action": "Choose a transport listed for this harness by the `cli harness list` runner operation.",
      "exitCode": 20,
      "retryable": false,
      "details": {
        "harness": "prime",
        "requested": "exec-json",
        "supported": ["rpc"]
      }
    }
  }
}
